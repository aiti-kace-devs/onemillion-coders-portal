tabs:
  main:
    fields:
      -
        handle: title
        field:
          type: text
          display: Title
          validate: required
      -
        handle: branch_id
        field:
          type: integer
          display: 'Branch Id'
          validate: required
      -
        handle: status
        field:
          type: text
          display: Status
  sidebar:
    fields: {  }
